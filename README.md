# トレーニングログ(スタンドアロン版)

Claudeを介さずに、ブラウザ単体で動くトレーニング・体重・食事の記録アプリです。
データは端末の localStorage に保存され、保存のたびに Google スプレッドシートへも自動で書き込まれます。
Claude の API は一切呼び出さないため、利用枠(usage)は消費しません。
基本機能(運動・体重・食事の記録、AIアドバイス、カロリー収支、内蔵の食品辞書など)はすべてルールベースで完結し、外部通信は発生しません。
**唯一の例外**が、任意で使える「🔍 AIで栄養を調べる」機能です。これだけは設定した場合に限り、Google Gemini APIに食品名を送って栄養価を推定します(詳しくは後述)。使わなければ今まで通り完全オフラインで動作します。

## 今回の更新内容 [v6](不具合修正: 食事の候補をタップしても検索文字が記録されてしまう)

- 「食事」タブや編集画面で食品名を検索して出てきた候補(🔍 概算カロリー候補)をタップしたとき、カロリーなどの数値は反映されるのに、記録される食事名は検索のために入力した文字のままになってしまう不具合を修正しました。今回から、候補をタップすると記録名もその候補の正式名(例:「ご飯(茶碗1杯)」)に置き換わるようになり、意図した食品名で正しく記録されます。
  - 「食事」タブの記録フォーム、「設定」タブの「よく食べるもの」登録フォーム、記録の編集画面(✏️)のすべてで同様に修正しています。
  - 候補をタップしたあとは候補一覧も消えるようにし、誤って別の候補をタップしてしまうことがないようにしました。

## 前回の更新内容 [v5](不具合修正: 「Load failed」エラーへの対策)

- 「同期」「🔍 AIで栄養を調べる」の両方で「Load failed」というエラーが出る不具合に対応しました。これはiPhoneのSafari(特にホーム画面に追加したアプリ)で、通信が一時的に失敗したときに出る汎用的なメッセージです。
  - 通信が失敗した場合、今回から少し時間を空けて自動で1回だけ再試行するようにしました。一時的な失敗であれば、これだけで自動的に回復します。
  - それでも失敗する場合は、エラーメッセージに「ネットワーク接続、またはApps ScriptのURL・デプロイ設定に問題がある可能性があります」という補足を添えて、次に何を確認すればよいか分かりやすくしました。
- もし再試行後も「Load failed」が続く場合は、Safariで直接そのApps ScriptのURLを開けるか(「このURLはPOST専用です」という表示が出れば正常)、デプロイ設定の「アクセスできるユーザー」が「全員」になっているかをご確認ください。

## 前回の更新内容 [v4](同期エラーの詳細を表示するように改善)

- Google Sheets同期が失敗したとき、これまでは「同期エラー」とだけ表示され、原因が分からない状態でした。今回から、「設定」タブの「同期」カードに、エラーの詳細(Apps Scriptから返ってきたメッセージ、または「応答がJSONではありません」といった具体的な原因)が表示されるようになりました。
  - 特に「応答がJSONではありません」と出た場合は、Apps Scriptのデプロイ設定で「アクセスできるユーザー」が「全員」になっていないことが典型的な原因です。デプロイの設定を再度ご確認ください。
  - このエラー詳細が分かれば、原因の切り分けがしやすくなります。

## 前回の更新内容 [v3](不具合修正: タイトルがステータスバーに隠れる)

- iPhoneのホーム画面から開いたとき、画面上部のタイトル(「🏋️ Training Log + AI」)が時計やバッテリー残量などのステータスバー・ノッチ部分に重なって隠れてしまう不具合を修正しました。端末ごとの安全領域(セーフエリア)分だけ自動で上に余白を空けるようにしたので、Dynamic Island搭載機種・ノッチ搭載機種・それ以外の機種のどれでも、その端末に合った分だけ正しく余白が入ります。

## 前回の更新内容 [v2](Google Sheets連携のURLを設定)

- `index.html` の `GAS_URL` に、実際にデプロイしたApps ScriptのウェブアプリURLを設定しました。GitHubに再アップロードすれば、記録するたびにGoogleスプレッドシートへ自動で書き込まれるようになります。
- 同じApps ScriptにスクリプトプロパティとしてGEMINI_API_KEYを追加すれば、「🔍 AIで栄養を調べる」機能も併せて使えるようになります(未追加でも他の機能には影響ありません)。

## 前回の更新内容 [v1](昨日コピーを食事区分ごとに・AIによる栄養価概算・バージョン表示)

これ以降、更新のたびにバージョン番号を付けます(設定タブの一番下に小さく表示されます)。

- **📋 昨日の記録コピーを、朝食・昼食・夕食・間食ごとに分けてコピーできるように変更**しました。「今日」タブのコピーカードで、運動は「🏋️ 運動」ボタン、食事は記録されている区分のボタン(例: 朝食・夕食)だけが表示され、その区分だけをコピーできます。「まとめて全部コピー」ボタンも引き続き使えます。
- **🔍 AIで栄養を調べる機能を追加(任意・要ネット接続)**: 「食事」タブの記録フォーム、「設定」タブの「よく食べるもの」登録フォーム、記録の編集画面(✏️)に「🔍 AIで栄養を調べる」ボタンを追加しました。内蔵の食品辞書(70品目)に候補がない食品でも、Google Gemini APIに食品名を送って、カロリー・タンパク質・脂質・炭水化物のおおよその値を取得できます。
  - **これを使うには追加の設定が必要です**: (1) Google Sheets連携(GAS_URL)の設定が済んでいること、(2) Google AI Studio(https://aistudio.google.com/apikey)で無料のGemini APIキーを取得し、Apps Scriptの「スクリプトプロパティ」に `GEMINI_API_KEY` として登録すること。手順は `apps-script.gs` のコメントにも記載しています。未設定の場合はボタンを押すと案内メッセージが表示されるだけで、他の機能には一切影響しません。
  - この機能を使ったときだけ、入力した食品名がGoogleのGemini APIに送信されます(それ以外の記録データは送信されません)。Gemini APIには無料枠がありますが、内容や利用量によっては将来的に条件が変わる可能性があります。心配な場合はこのボタンを使わず、これまで通り内蔵辞書と手入力だけで使い続けられます。

## 前回の更新内容(毎日の入力を楽にする4つの機能を追加)

日々の記録をワンタップに近づけるため、以下を追加しました。

- **🔁 ワンタップ再記録**: 「食事の履歴」「運動の履歴」の各行に🔁ボタンを追加しました。タップすると、その記録と同じ内容を今日の日付で複製します。いつもの食事・いつもの運動を毎回手入力し直す必要がなくなります(元の記録はそのまま残ります)。
- **📋 昨日の記録をコピー**: 「今日」タブの上部に、昨日トレーニング・食事の記録があるときだけ「📋 昨日の記録をコピー」カードが表示されます。タップすると、昨日の内容(トレーニング・食事すべて)をまとめて今日にコピーします。生活リズムがほぼ同じ方向けの一括操作です。
- **食事タイプの自動選択**: 「食事」タブを開いたとき、現在時刻に応じて朝食(4-10時)・昼食(10-15時)・間食(15-18時)・夕食(18-4時)のチップを自動で選んでおくようにしました。毎回チップをタップする手間が減ります(もちろん手で選び直せます)。
- **📋 よく使う運動メニューを登録**: 「運動」タブに、今日すでに記録した種目をまとめて名前を付けて保存できる「よく使う運動メニュー」機能を追加しました。「いつもの胸トレ」のように保存しておくと、次回から「▶️ 実行」をワンタップするだけで、その日に行った複数種目・セットをまとめて記録できます。
- これらはすべて手元のデータをそのまま複製・整理するだけの機能で、外部AI・外部APIへの通信は発生しません(利用枠は消費しません)。

## 前回の更新内容(不具合修正: 食事の履歴が見られない・過去の食事が「今日」に紛れて見える)

- 「食事」タブに、日付ごとにまとめて過去の食事を確認できる **「📖 食事の履歴」カード** を追加しました。これまでは記録フォームの下に、日付表示のない食事の一覧が最大8件だけ雑然と並んでいたため、過去の日付で入力した食事がどれなのか分からず、実際にはきちんと保存されているのに「今日」に混ざって見えてしまう状態でした。今回から日付(と曜日、その日の合計kcal)ごとに区切って表示されるので、過去に入力した食事もすぐ見つけられます(直近14日分)。
  - 実際のデータはこれまでも入力した日付で正しく保存されていました(バグではなく表示上の問題でした)が、紛らわしかった点は修正しています。
- 「運動」タブの「履歴」(リスト表示)に、これまでトレーニングの記録がない日は出てこず、体重や食事だけ記録した日が抜け落ちる不具合がありました。今回から、トレーニング・体重・食事のいずれかを記録した日はすべて一覧に表示され、その日の記録内容(トレーニング・体重・食事)がまとめて確認できるようになりました。

## 前回の更新内容(不具合修正: トレーニングした日にカロリー目標が増えないのを修正)

- 「今日は運動したのに、食べていいカロリーの目標が増えない」という点を修正しました。これまでは「今日」タブ・「食事」タブに表示される目標カロリーが、体重目標から計算した固定値(TDEE±増減)のみで、その日にどれだけ運動したかが反映されていませんでした(カロリー収支グラフの内部計算では考慮されていましたが、目標の数字自体には反映されていませんでした)。
- 今回から、「今日」「食事」タブの目標カロリーに、その日ログした運動の推定消費カロリー(筋トレ1セットあたり概算30kcal、有酸素は運動強度(METs)×体重×時間)を上乗せするようにしました。トレーニングを記録した日は、その分だけ目標カロリーの数字がその場で増えます。
- 目標カロリーの下に「TDEE ○○kcal + 今日の運動 ○○kcal を目標に加算しています」という内訳を表示し、増えた理由が分かるようにしました。運動を記録していない日は今まで通りの表示のままです。
- 🤖「今日のひとこと」のアドバイス文にも、運動で消費が増えた分のコメントを追加しています。
- この機能もルールベースの計算のみで、外部AI・外部APIへの通信は一切発生しません(利用枠は消費しません)。

## 前回の更新内容(配色を青系の明るいテーマに変更)

- アプリ全体の配色を、暖色系(ベージュ×テラコッタ)から青系の明るいテーマに変更しました。背景は薄い水色、メインカラーはビジュアルの中心となる明るいブルーです。
- 削除ボタンやエラー表示、注意を促すメーター(カロリー超過など)は、視認性のためこれまで通り暖色(コーラル)を残しています。全部を青にしてしまうと「危険・注意」の意味が伝わりにくくなるための意図的な判断です。もし警告系の色も含めて完全に青系へ統一したい場合は教えてください。
- 色の変更は `<style>` 内のCSS変数(`--paper`, `--ink`, `--teal`, `--sand`, `--slate` など)にまとまっているので、今後好みの色合いに調整したい場合もここを書き換えるだけで反映されます。

## 今回の更新内容(不具合修正: 体重タブの入力場所が分かりにくい)

- 「体重」タブで、どこに数値を入力すればいいか分かりにくかった点を修正しました。グラフのカードと入力フォームのカードを分け、「体重を記録」という見出しと、「日付」「体重 (kg)」というラベルを常時表示するようにしました。記録ボタンも目立つ横長の「＋ 記録する」ボタンに変更しています。

## 前回の更新内容(UI改善: 入力をシンプルに)

アプリ全体の入力の手間を減らす目的で、以下を変更しました。

- **「食事」タブ: カロリー・P/F/C入力を折りたたみ式に。** 普段は「食事名+区分+日付」だけのシンプルな画面になり、「詳細を入力(カロリー・栄養素・任意)」をタップしたときだけ数値入力欄が開きます。よく食べるものプリセットやカロリー概算候補をタップしたときは、入力された値がすぐ見えるように自動で開きます。
- **「体重」タブ: 直近の体重を自動で入力欄に表示。** 毎日ほぼ同じ体重を入力する手間を減らすため、前回記録した体重があらかじめ入っています。変化があった分だけ書き換えればOKです。
- **「運動」タブ(筋トレ): 負荷(kg)をドロップダウンから－/＋ボタンに変更。** 2.5kg刻みでワンタップで調整できます。回数もよく使う数値(5・8・10・12・15・20回)をチップからワンタップで選べるようにしました。
- **前回の回数・負荷・時間を記憶。** 同じ種目(または部位)を選ぶと、前回入力した回数・負荷が自動で入るようになりました。有酸素運動も、同じ種目を選ぶと前回の時間が自動で入ります。手で書き換えればその値が優先されるので、上書きされる心配はありません。
- **「設定」タブをセクション折りたたみ式に。** よく使う「同期」「目標」はこれまで通り常に表示し、「AIアドバイス用プロフィール」「マスタ管理」「データ」は普段は畳んだ状態にして、スクロール量を大きく減らしました。見出しをタップすると開閉できます。

## 前回の更新内容(「今日のおすすめ」にジムトレ・ウォーキング+筋トレの組み合わせを追加)

- 「今日」タブの🤖「今日のおすすめ」で提案される運動のバリエーションを増やしました。これまでは有酸素運動(ウォーキング・ジョギングなど)や自重トレーニングだけでしたが、以下のパターンも提案されるようになりました。
  - **ジムでのトレーニング**: 「ジムで胸: ベンチプレス 4セット×10回」のように、部位と種目・セット数を提案します。種目名は「設定」タブの種目マスタに登録した内容(ジムのマシン名など)があればそちらを優先して提案し、未登録なら標準のおすすめ種目から選ばれます。
  - **ウォーキング+筋トレの組み合わせ**: 「ウォーキング15分 + 脚(スクワット) 3セット×15回」のように、有酸素と部位別トレーニングを組み合わせて提案します。
  - 提案は気分(疲れた/ふつう/元気)と1日に使える時間に応じて自動で選ばれ、これまで通り日付ごとに決まったローテーションで変化します。
  - 「記録する」ボタンをタップすると、ジム提案は「運動」タブの筋トレフォーム(部位・回数)に、組み合わせ提案はまず有酸素側(ウォーキングなど)がフォームに入力されます。組み合わせの筋トレ側は、提案テキストを見ながら別途「筋トレ」サブタブから記録してください。
  - この機能もルールベースのみで動作しており、外部AI・外部APIへの通信は発生しません(利用枠は消費しません)。

## 前回の更新内容(不具合修正: AIプロフィールの年齢・身長のラベル表示)

- 「設定」タブの🤖AIアドバイス用プロフィールで、年齢・身長の入力欄にラベル(項目名)が表示されていなかった不具合を修正しました。今までは薄いプレースホルダー文字だけだったため、数値を入力すると何の欄か分からなくなっていました。他の入力欄と同じように「年齢」「身長 (cm)」の見出しを常時表示するようにしています。

## 前回の更新内容(カロリー収支の推移グラフ+アドバイス機能を追加)

- **「今日」タブに📉「カロリー収支の推移(直近14日)」カードを追加しました。** その日の摂取カロリー(食事タブの記録)と、TDEE(1日の推定消費カロリー)+その日の運動による消費カロリーの概算との差額を、日ごとの棒グラフで表示します。
  - 青(🟦)の棒は消費が上回った日(赤字=減量方向)、オレンジ(🟧)の棒は摂取が上回った日(黒字=増量方向)です。食事を記録していない日は横棒で表示されます。
  - 運動による消費カロリーは、筋トレは1セットあたり概算30kcal、有酸素運動は種目ごとの運動強度(METs)×体重×時間から簡易計算しています(いずれも目安の値です)。
  - グラフの下に、直近の平均収支と目標体重(減量/増量/維持)の方向性をふまえて、応援コメント(👍順調なペースです、など)または注意コメント(⚠️赤字が大きすぎます、黒字気味です、など)が表示されます。
  - この機能もルールベースの計算のみで、外部AI・外部APIへの通信は一切発生しません(利用枠は消費しません)。医療的な正確性は保証されないため、体調に不安がある場合は専門家にもご相談ください。

## 前回の更新内容(食事名からカロリーを概算する機能を追加)

- 食事の名前を入力すると、アプリに内蔵した食品カロリー概算表(約70品目・外部APIへの接続なし)から近い候補を自動で提案するようになりました。候補をタップすると、カロリー・タンパク質・脂質・炭水化物がまとめて入力欄に入ります(数値はあくまで目安なので、正確な値がわかる場合は上書きしてください)。
  - 「食事」タブの記録フォーム、「設定」タブの「よく食べるもの」マスタ登録フォーム、記録の編集画面(✏️)の3か所すべてで使えます。
  - 「鶏むね」→「鶏むね肉」、「むね肉」のような表記ゆれ・部分一致にもある程度対応しています。完全に一致しない食品は候補に出ないので、その場合は今まで通り手入力してください。
  - この機能もルールベースの内蔵辞書のみで動作しており、外部AI・外部APIへの通信は発生しません(利用枠は消費しません)。

## 前回の更新内容(1日に使える時間に「1時間」を追加)

- 🤖AIアドバイス用プロフィールの「1日に使える時間」の選択肢に **「1時間」** を追加しました(設定タブ・初回オンボーディングの両方)。長めに運動時間を確保できる方向けの選択肢です。「30分以上」と同様、おすすめ運動は最も負荷の高いメニューから選ばれます。

## 前回の更新内容(マスタ登録機能を追加)

- **「設定」タブに🗂️マスタ管理を追加**: 筋トレの種目(ジムのマシン名など)と、よく食べる食事をあらかじめ登録できるようになりました。
  - 筋トレ種目は部位ごとに登録・削除ができます。初回は今までの「おすすめ種目」がそのまま登録済みの状態になっているので、自由に編集・追加・削除してください。
  - 食事は名前・カロリー・P/F/C(タンパク質/脂質/炭水化物)を登録できます。
  - 「すべてのデータを削除」をすると、筋トレ種目はデフォルトのおすすめ内容に戻り、食事のマスタは空になります。
- **「運動」タブの種目チップがマスタ連動に**: 部位を選ぶと、その部位に登録されている種目がチップとして表示されます(未登録の部位はその旨が表示され、設定タブから追加できます)。記録の編集画面でも同じチップが使えます。
- **「食事」タブに「よく食べるもの」チップを追加**: マスタに登録した食事をタップすると、名前・カロリー・P/F/Cが記録フォームに自動入力されます(あとは日付を確認して「記録する」を押すだけ)。

## 前回の更新内容(UIを全面リニューアル)

別途プロトタイプ検討していた「AIダイエットアシスタント(ai-diet-mvp)」のUI・画面構成をベースに、こちらのアプリの機能(記録編集・カレンダー・部位別種目チップ・Google Sheets同期)をすべて統合しました。

- **タブ構成に変更**: 画面下部のタブバーで 今日 / 食事 / 体重 / 運動 / 設定 を切り替える形になりました。以前の「1ページに全部並ぶ」形式から、ai-diet-mvp式の画面構成になっています。
- **初回オンボーディング**: 初めて開いたとき(またはデータを全削除した後)に、年齢・性別・身長・体重・目標体重・運動習慣・使える時間・週間トレーニング目標を入力する簡単なウィザードが表示されます。既存のトレーニング・体重・食事の記録はそのまま引き継がれます。
- **「運動」タブ**: 歩数トラッキングの代わりに、筋トレ・有酸素の記録(部位別のおすすめ種目チップつき)・進捗グラフ(週間頻度・種目別重量推移)・履歴(リスト/カレンダー切り替え)をこのタブにまとめました。
- **「今日」タブ**: 連続記録日数、🤖 AIのひとこと(気分に応じたアドバイス+おすすめ運動、タップで運動タブに自動入力)、体重目標・週間トレーニング目標・今日のカロリーの達成状況をひと目で確認できます。
- **「設定」タブ**: Google Sheets同期状況、体重目標・週間トレーニング目標、AIアドバイス用プロフィール(年齢・身長・性別・運動習慣・使える時間、Mifflin-St Jeor式での目標カロリー・PFC計算)、データ削除をまとめています。
- 記録の編集(✏️)、部位ごとの種目チップ、Google Sheets同期・GitHub Pagesホスティングは変わらず利用できます。「食べ過ぎても怒らないAIチャット」は今回は含めていません。

## ファイル構成

- `index.html` … アプリ本体(このファイル1つだけで動作します)
- `apps-script.gs` … Googleスプレッドシートに書き込むための Google Apps Script コード
- `README.md` … このファイル

## セットアップ手順

### 1. 記録用のGoogleスプレッドシートを作る

1. [Google スプレッドシート](https://sheets.google.com) で新規シートを作成し、好きな名前を付ける(例:「トレーニングログ」)。
2. メニューから **拡張機能 → Apps Script** を開く。
3. デフォルトで入っているコードを全部消して、`apps-script.gs` の中身を貼り付けて保存する。
4. 画面右上の **デプロイ → 新しいデプロイ** をクリック。
5. 種類の選択で歯車アイコンから **ウェブアプリ** を選ぶ。
6. 設定:
   - 実行するユーザー: **自分**
   - アクセスできるユーザー: **全員**
7. **デプロイ** をクリックし、Googleアカウントでの承認を求められたら許可する。
8. 発行された **ウェブアプリのURL**(`https://script.google.com/macros/s/.../exec` の形式)をコピーしておく。

### 2. index.html にURLを設定する

`index.html` の冒頭付近にある以下の行を、手順1でコピーしたURLに書き換える。

```js
const GAS_URL = "PUT_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE";
```

### 3. ホスティングする(iPhoneのホーム画面に追加するため)

ローカルファイルのままだとiPhoneの「ホーム画面に追加」が安定しないため、どこかに公開してURLを持たせるのがおすすめです。手軽な方法をいくつか挙げます。

**GitHub Pages(無料・おすすめ)**
1. GitHubに新しいリポジトリを作る(例: `training-log`)。
2. このフォルダの `index.html` をリポジトリのルートに置いてpushする。
3. リポジトリの Settings → Pages で、公開元をそのブランチ(例: `main`)のルートに設定する。
4. 数分後、`https://<ユーザー名>.github.io/training-log/` のようなURLでアクセスできるようになる。

Claude Codeを使う場合、`git init` → `gh repo create` → `git push` → Pages設定、までを一通り自動化してもらうこともできます。

**Google Cloud Run**
すでにfreeeの自動化などでCloud Runを使っているなら、静的ファイル配信用のコンテナ(nginxなど)でこの `index.html` を配信することもできます。GitHub Pagesより一手間増えますが、他のインフラと管理を一本化したい場合はこちらでも構いません。

### 4. iPhoneのホーム画面に追加する

1. 公開したURLをiPhoneのSafariで開く。
2. 共有ボタン(□に↑のアイコン)をタップ。
3. 「ホーム画面に追加」を選択。

これでアイコンをタップするだけでフルスクリーンのアプリのように起動します。

## 動作の仕組み

- 記録の追加・削除のたびに、まず端末の localStorage に即座に保存します(ネット接続不要)。
- その直後にバックグラウンドで Apps Script の Web アプリ URL に記録全体を送信し、スプレッドシートの内容を丸ごと上書きします。
- 通信に失敗しても localStorage 側のデータは失われません。画面上部の同期ステータスをタップすると再試行できます。

## 既知の制限・カスタマイズ余地

- 現状、複数端末間でのデータ共有は「都度スプレッドシートに書き込む」一方向のみです(スプレッドシート側からアプリへの読み込みは未実装)。複数端末で使いたい場合は、起動時にスプレッドシートからデータを読み込む処理を追加すると良いです。
- ホーム画面用のアイコン画像(`apple-touch-icon`)は未設定です。好みの画像を用意して `<link rel="apple-touch-icon" href="icon.png">` をheadに追加すると、アイコンが見た目良くなります。
- デザイン(色・フォント)はai-diet-mvpのトーンに合わせています。調整したい場合は `<style>` 内のCSS変数(`--teal`, `--coral`, `--sand`, `--slate` など)を変更してください。
- AIによるアドバイス・目標カロリー計算はすべてルールベースの簡易ロジックで、外部AI APIには接続していません。医療的な正確性は保証されないため、実際の健康管理では専門家の助言も参考にしてください。
